#ifndef system_state_variables.h
#define system_state_variables.h

// Using euler angles to describe the orientation of the object (note that this is subject to gimball locking)
extern float alpha = 0;      // angle for x
extern float beta = 0;       // angle for y
extern float gamma = 0;      // angle for z

#endif